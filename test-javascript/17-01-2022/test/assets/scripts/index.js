import { data } from './data.js';

const main = document.querySelector("div#article-list[data-lang=\"en\"]");

const Test = () => {
  const [open, setOpen] = (false);
  const refBoxMenu = useRef(null);

  useEffect(() => {
    console.log(anchorEl)
  }, [anchorEl]);

  return (

  )
}